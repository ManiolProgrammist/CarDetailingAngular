import { OrderTemplateImage } from './order-template-image.model';

describe('OrderTemplateImage', () => {
  it('should create an instance', () => {
    expect(new OrderTemplateImage()).toBeTruthy();
  });
});
